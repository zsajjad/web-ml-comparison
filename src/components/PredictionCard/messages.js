/*
 * PredictionCard Messages
 *
 * This contains all the text for the PredictionCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PredictionCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PredictionCard component!',
  },
});
